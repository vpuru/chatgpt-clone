import "./globals.css";

export const metadata = {
  title: "ChatGPT Clone",
  description: "A production-grade chat experience"
};

export default function RootLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-slate-950 text-slate-50">
        {children}
      </body>
    </html>
  );
}
